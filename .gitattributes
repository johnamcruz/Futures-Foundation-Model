checkpoints/*.pt filter=lfs diff=lfs merge=lfs -text
# EXCEPTIONS: the base checkpoints Colab needs must stay RAW blobs — a plain `git clone` (no
# git-lfs in the runtime) still materializes them, so BASE_BACKBONE_CKPT is never a dead pointer.
checkpoints/mantis_ssl_ohlcv.pt   -filter -diff -merge -text
checkpoints/mantis_ssl_seq2seq.pt -filter -diff -merge -text
