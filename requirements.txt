torch>=2.0
transformers>=4.41,<5      # Chronos-2 supported; Transformers 5 conflicts with Mantis safetensors
peft>=0.10             # bolt_finetune --lora: parameter-efficient Chronos-Bolt fine-tune
datasets>=2.14
safetensors>=0.4,<0.5      # shared compatibility: mantis-tsfm 1.0 + Chronos-2
pandas>=2.0
numpy>=1.24,<2          # torch 2.x built against numpy 1.x ABI (_ARRAY_API); numpy 2 breaks DataLoader workers
scikit-learn>=1.3
pyyaml>=6.0
xgboost>=2.0
# ONNX export (produce export_onnx=True): XGBoost heads + Chronos encoder → ONNX
onnxmltools>=1.16
onnxruntime>=1.16
onnx>=1.16
onnxscript>=0.1
optuna>=3.0
joblib>=1.3
hmmlearn>=0.3              # regime HMM (futures_foundation.regime): market-state posteriors as head features
chronos-forecasting>=2.2   # Chronos-2 multivariate + LoRA and legacy Chronos-Bolt
databento>=0.76            # DBN metadata, decoding, and symbology
zstandard>=0.23            # compressed Databento CSV input
ml-training-loop @ git+https://github.com/johnamcruz/ML-training-loop.git@0b31183cbe28141269fb5a250913cbdccac48d27
